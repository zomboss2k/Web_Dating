import React from 'react'
import { Carousel } from "react-bootstrap";

const Match = ({user: {_id, username, fullname, email, password, avatar, age, gender, mobile, address, images, desc, createdAt, updatedAt, __v}}) => {
  return (
    <></>
  )
}

export default Match